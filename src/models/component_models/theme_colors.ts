export interface ThemeColors {
  background: string;
  text: string;
  placeholder: string;
  border: string;
  error: string;
  buttonBackground: string;
  buttonText: string;
  required: string;
  inputBackground: string;
}
