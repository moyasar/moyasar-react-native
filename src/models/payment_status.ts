export enum PaymentStatus {
  initiated = 'initiated',
  paid = 'paid',
  failed = 'failed',
  authorized = 'authorized',
  captured = 'captured',
  refunded = 'refunded',
  voided = 'voided',
  verified = 'verified',
}
