import { StyleSheet } from 'react-native';

export const moyasarLogoStyles = StyleSheet.create({
  moyasarLogo: {
    paddingTop: 20,
    alignItems: 'center',
    alignSelf: 'center',
    height: 40,
  },
});
