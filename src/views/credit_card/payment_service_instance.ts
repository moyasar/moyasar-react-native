import { CreditCardPaymentService } from '../../services/credit_card_payment_service';

// TODO: Modify to a better approach rather than global variable
export const paymentService = new CreditCardPaymentService();
